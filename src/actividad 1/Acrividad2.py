# Calcular el aera de un triangulo

base = input ("Ingrese la base del triangulo: ")

altura = input ("Ingrese la altura del triangulo: ")

area = (float(base) * float(altura)) / 2

print ("El area del triangulo es: " + str(area))